apiVersion: v1
kind: Service
metadata:
  name: web
spec:
  selector:
    app.kubernetes.io/name: evalpulse
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 3000
      targetPort: http

