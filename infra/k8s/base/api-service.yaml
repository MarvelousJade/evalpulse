apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  selector:
    app.kubernetes.io/name: evalpulse
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 8000
      targetPort: http

